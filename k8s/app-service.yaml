apiVersion: v1
kind: Service
metadata:
  name: parkkbus-service
  namespace: parkkbus
spec:
  selector:
    app: parkkbus-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer

